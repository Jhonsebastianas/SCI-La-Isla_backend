export const enum MagicNumber {
    CERO = 0,
    UNO = 1,
    DOS = 2,
    TRES = 3,
    CUATRO = 4,
    CINCO = 5,
}

export const enum NombrePlantilla {
    ACTIVAR_CUENTA = "activar-cuenta",
    RECUPERAR_CUENTA = "restablecer-clave",
}

export const enum TipoRegistro {
    MANUAL = "manual",
    SENSOR = "sensor",
}

export const enum TipoServicioSecundario {
    NN = "nn",
}

export const enum TipoAutor {
    ANONIMO = "Anónimo",
}

export const enum TipoDato {
    TIP = "TIP",
}